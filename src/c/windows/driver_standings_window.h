#pragma once

#include <pebble.h>

// Create and show the driver standings window
void driver_standings_window_push(void);

// Destroy the driver standings window
void driver_standings_window_destroy(void);
