#pragma once

#include <pebble.h>

// Create and show the qualifying results window
void results_qualifying_window_push(int race_round);

// Destroy the qualifying results window
void results_qualifying_window_destroy(void);
